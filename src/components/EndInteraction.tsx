import { Button } from "@material-ui/core";

export const EndInteraction = ():JSX.Element => {
    return (
        <Button variant="contained" color="secondary" size="large" style={{minWidth:"25%", marginRight:"20px"}}>
            END INTERACTION
        </Button>
    )
}